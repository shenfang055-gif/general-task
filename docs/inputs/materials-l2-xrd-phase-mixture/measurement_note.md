# Measurement note

The peak list is a synthetic powder XRD measurement after background subtraction. Match reference peaks within `±0.15° 2θ`. Treat observed peaks below intensity `5` as noise. Estimate non-negative phase fractions that sum to one; the scale of each reference pattern is arbitrary, so fit one common observed scale together with the fractions. A reference phase with no supported peak set should not be reported as present.

